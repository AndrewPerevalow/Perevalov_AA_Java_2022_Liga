spring:
  flyway:
    user: andrey
    password: pass
    schemas: database1
    url: jdbc:postgresql://localhost:5400/database1
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    username: andrey
    password: pass
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5400/database1
statuses:
  default-status: Новое
  work-status: В работе
  complete-status: Готово